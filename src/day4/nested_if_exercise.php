<?php
// 🎯 Day4 練習問題：ネストされた条件（nested_if_exercise.php）

// 📝 【問題】
// あなたは遊園地の入場判定をするプログラムを作ります。
// 以下のルールに従って、「入場可能」または「入場不可」とブラウザに表示してください。

//
// ✅ ルール：
// 1. 年齢が13歳以上なら → 「チケットを持っているか？」を確認
// 2. 年齢が13歳未満なら → 「保護者の同伴があるか？」を確認
//
// - 13歳以上 & チケットを持っている → 「入場可能」
// - 13歳以上 & チケットを持っていない → 「入場不可」
// - 13歳未満 & 保護者同伴 → 「入場可能」
// - 13歳未満 & 保護者なし → 「入場不可」
//

// ✅ 変数は以下のように用意してください：
$age = 12; // 年齢（自由に変更OK）
$has_ticket = false; // チケットを持っているか（true または false）
$with_parent = true; // 保護者が同伴しているか（true または false）

// 🔰 ここに if の中に if を使った条件分岐を記述してください。

// 📝 出力形式は <br> を使い「結果: ○○」と表示してください。


// 🔍 ヒント
// $age が 13 以上か未満でまず分岐
// その中でさらに「チケット」「保護者」の条件を確認
// if の中に if を書くネスト形式で実装！

// ✍️ 完成イメージ（動作確認の例）
// もし $age = 12; $has_ticket = false; $with_parent = true; なら
// 👉 結果: 入場可能 とブラウザに表示される

// ----------------------------

// ✅ 日本語の処理手順を書いて → コメント化 → コード化

// 💡 ヒント：処理手順の考え方
// 1️⃣ まず年齢で2つに分ける
// 　👉 「13歳以上」か「13歳未満」か

// 2️⃣ それぞれの条件の中で、さらに条件を確認する
// 　👉 13歳以上 → 「チケットを持っているか？」
// 　👉 13歳未満 → 「保護者が同伴しているか？」

// 3️⃣ 条件ごとに「入場可能」「入場不可」を出力する



$age = 12; // 年齢（自由に変更OK）
$has_ticket = true; // チケットを持っているか（true または false）
$with_parent = true; // 保護者が同伴しているか（true または false）

// ✅ この日本語処理をPHPのコメントとして書くのが最初のステップになります。
// まず自分で考えて入力してみる。
//    ↓

// もし年齢が13歳以上なら
// if ($age >=13) {
// もしチケットを持っているなら
    // if($has_ticket) {
//     「入場可能」と出力
        // echo "入場可能<br>";
//   そうでないなら
//  「入場不可」と出力
    // } else {
    //     echo "入場不可<br>";
    // } else {

// そうでないなら（年齢が13歳未満なら）
    // if ($age < 13) {
//   もし保護者が同伴しているなら「入場可能」と出力
    // if ($with_parent) {
    //     echo "入場可能<br>";
//   そうでないなら「入場不可」と出力
//     } else {
//         echo "入場不可<br>";
//     }
// }
// }

// ここまで書いてみて
// 修正に入る。
//       ↓

// もし年齢が13歳以上なら
if ($age >= 13) {
    // もしチケットを持っているなら
    if ($has_ticket) {
        // 「入場可能」と出力
        echo "結果: 入場可能<br>";
    } else //（チケットがない）
        {
        // 「入場不可」と出力
        echo "結果: 入場不可<br>";
    }
//     ↑
// ここまでが「13歳以上」部分

// ↓  ここからが「13歳未満」
// 外側の「if」に対しての
// 「else（そうでない）」になる
} else { // そうでないなら（年齢が13歳未満なら）
    // もし保護者が同伴しているなら
    if ($with_parent) {
        // 「入場可能」と出力
        echo "結果: 入場可能<br>";
    } else //同伴じゃない
    {
        // 「入場不可」と出力
        echo "結果: 入場不可<br>";
    }
}

