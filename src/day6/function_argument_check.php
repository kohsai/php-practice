<?php
// 🎯 Day6 練習問題（function_argument_check.php）

// 📝 【問題】
// 関数の引数と、関数を呼び出すときの変数の名前が異なっていても、
// 処理が正しく動作することを確認してください。

// 【条件】
// ✅ 関数 `showMessage($msg)` を定義し、引数 `$msg` を使ってメッセージを表示する
// ✅ 関数の中では「受け取ったメッセージ：○○」と表示する（<br>付き）
// ✅ 関数を3回呼び出す。それぞれ異なる変数（例：$text1, $info, $message）を使ってメッセージを渡す
// ✅ 出力例：「受け取ったメッセージ：こんにちは！」

// 【ヒント】
// 💡 関数の引数名（$msg）と、呼び出す側の変数名（$text1など）は一致していなくてもOKです。


// ✅ ここから下に『自分で考えた日本語の処理手順』をコメントで書いてください。
// （ここに自分で日本語の処理文を書いてください）
//  関数 showMessageを定義する。
//  関数の中で「受け取ったメッセージ：○○」と表示する。
//  関数を三回呼び出す。


// ✅ その下に『自分でPHPコード』を書いてください。
// （ここに自分でPHPコードを書いてください）
function showMessage($msg)
{
    echo "受け取ったメッセージ:{$msg}";
}

echo "【関数に関する理解を深めるテスト】<br><br>";

// $text1, $info, $messageを使う
$text1 = "練習問題<br>";
showMessage($text1);

$info = "確認しましょう<br>";
showMessage($info);

$message = "よくできました！<br>";
showMessage($message);
