<?php
// 🎯 Day6 練習問題（function_return_message.php）

// 📝 【問題】
// 名前を受け取って、あいさつ文を return する関数を作成してください。
// 戻り値として返されたメッセージを使って、画面に表示してください。

// 【条件】
// ✅ 関数名は `createGreeting` とする
// ✅ 引数に「名前（文字列）」を1つ受け取る
// ✅ 「○○さん、こんにちは！」という形式の文字列を return する（○○には引数の名前が入る）
// ✅ 関数を1回呼び出し、戻り値を画面に表示する
// ✅ 表示形式は1行で `<br>` を使用する

// 【ヒント】
// 💡 return の中で文字列を組み立てて返します（連結や埋め込みに慣れましょう）
// 💡 関数内では echo しないように注意（return で渡す）

// ✅ ここから下に『自分で考えた日本語の処理手順』をコメントで書いてください。
// （ここに自分で日本語の処理文を書いてください）
//  関数名 createGreeting を定義。
//  引数に「名前」を一つ受け取る。
//  「○○さん、こんにちは！」という文字列をreturnする。
//  関数を一回呼び出し、戻り値を画面に表示する。


// ✅ その下に『自分でPHPコード』を書いてください。
// （ここに自分でPHPコードを書いてください）
function createGreeting()
{
    return "venさん、こんにちは！";
}

$message = createGreeting();

echo $message . "<br>";


// ここで採点に入り、問題文の理解の不足に気づく。
//             ↓

// 名前（引数）を受け取っているか	❌
// ※今回は 引数を受け取っていません（空欄）。
// ⇒ function createGreeting($name) のように引数を受け取る必要があります。

// 「○○さん、こんにちは！」の形式で return	⭕️
// 固定文字列を返しており構文としてはOK。ただし、引数を反映していない点が惜しいです。

// ❗️修正ポイントの解説

// function createGreeting($name) {
//     return "{$name}さん、こんにちは！";
// }

// $message = createGreeting("KOH");
// echo $message . "<br>";

// ✅ このようにすれば「引数を使ってメッセージを作る」構造が成立し、
// 本来の問題の趣旨（文字列の return + 引数の活用）を満たせます。


// ✅ いまの練習問題のケースでいうと…

// function createGreeting() {
//     return "venさん、こんにちは！";
// }
// は、「誰に対しても“venさん”と表示」するので、引数なしでも動作します。

// ただし、問題では：
// 「○○さん、こんにちは！」（○○部分は外から渡す名前）

// という条件だったため、名前を引数として受け取る必要があったのです。

// function createGreeting($name) {
//     return "{$name}さん、こんにちは！";
// }
