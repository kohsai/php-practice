<?php
// 🎯 Day6 練習問題（function_return_check_practice.php）

// 📝 【問題】
// 2つの数値を受け取って、どちらが大きいかを判定する関数を作成してください。
// 関数は、より大きい方の数値を戻り値として返すようにします。
// 呼び出し元では、その戻り値を使って「大きい方の数値は ○○ です」と表示してください。

// 【条件】
// ✅ 関数名は getMaxNumber とする
// ✅ 引数に2つの整数を受け取る（例：5 と 8）
// ✅ どちらが大きいかを比較し、大きい方の数を return する
// ✅ 呼び出し元では戻り値を受け取り、次の形式で表示する（<br> 使用）
//    → 「大きい方の数値は ○○ です」
// ✅ 関数を2回以上呼び出して、異なる数値で確認すること

// 【ヒント】
// 💡 if 文を使って大きい方を比較しましょう
// 💡 return された値を受け取って、echo で表示します


// 1️⃣ ✅ ここから下に『自分で考えた日本語の処理手順』をコメントで書いてください。

// （ここに自分で日本語の処理文を書いてください）
// 関数名は getMaxNumber と定義する
// 引数に2つの整数を受け取る（例：5 と 8）
// どちらが大きいかを比較し、大きい方の数を return する
// 呼び出し元で戻り値を受け取り以下のように表示する（<br> 使用）
//    → 「大きい方の数値は ○○ です」
// 関数を2回以上呼び出して、異なる数値で確認すること


// 💡【思い出しヒント】
// 【1】if文の復習

// if ($a > $b) {
//     // $aが大きいときの処理
// } else {
//     // $bが大きい（または同じ）ときの処理
// }


// 【2】return の使い方（Day6より）

// function add($a, $b) {
//     return $a + $b; // 戻り値を返す
// }

// $result = add(3, 5); // ← 戻り値を受け取る
// echo "合計は {$result} です<br>";


// 2️⃣ ✅ その下に『自分でPHPコード』を書いてください。

// （ここに自分でPHPコードを書いてください）

// 関数の定義
function getMaxNumber($num1, $num2) {

// if文を使って比較（どちらが大きいか）
    if ($num1 > $num2) {
        return $num1;
    } else {
        return $num2;
    }
}

// 関数を呼び出して戻り値を受け取る
$max1 = getMaxNumber(7, 3);
echo "大きい方の数値は {$max1} です<br>";

// もう1回別の引数で呼び出して確認
$max2 = getMaxNumber(6, 9);
echo "大きい方の数値は {$max2} です<br>";
