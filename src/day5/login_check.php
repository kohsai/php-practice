<?php
// 🎯 Day5 練習問題（login_check.php）
//
// 📝 【問題】
// 入力されたユーザー名とパスワードをチェックして、ログインの可否を判定するプログラムを作成してください。
//
// 【条件】
// ✅ 変数 `$input_name` にユーザーが入力した名前（例："koh"）を代入
// ✅ 変数 `$input_pass` にユーザーが入力したパスワード（例："1234"）を代入
// ✅ 正しい組み合わせ（例：名前が "koh"、パスワードが "1234"）なら → 「ログイン成功」と表示
// ✅ それ以外なら → 「ログイン失敗」と表示
// ✅ 出力は1行で `<br>` を使ってブラウザに表示する
//
// 【ヒント】
// 💡 `if` を使って「名前とパスワードが両方一致」しているかをチェックしましょう
//

// ここから下に『自分で考えた日本語の処理手順』をコメントで書いてください。
// （ここに自分で日本語の処理文を書いてください）

// 変数 `$input_name` にユーザーが入力した名前（例："koh"）を代入
// 変数 `$input_pass` にユーザーが入力したパスワード（例："1234"）を代入
// 正しい組み合わせ（例：名前が "koh"、パスワードが "1234"）なら → 「ログイン成功」と表示
// それ以外なら → 「ログイン失敗」と表示
// 出力は1行で `<br>` を使ってブラウザに表示する


// ✅ その下に『自分でPHPコード』を書いてください。
// （ここに自分でPHPコードを書いてください）
$input_name = "koh";
$input_pass = "1234";

if ($input_name && $input_pass) {
    echo "ログイン成功<br>";
} else {
    echo "ログイン失敗<br>";
}


// 🔍 採点ポイントチェック
// 採点項目	判定	コメント
// $input_nameと$input_passの定義	✅	OKです！変数に値を代入できています。
// 正しい組み合わせの判定	❌	条件が間違っています。
// 実際の「比較（===）」が必要です。
// 「それ以外」の処理	❌	ここも「正しい組み合わせでなかった場合」を判定していません。
// <br>による改行表示	✅	OK！表示形式も守れています。

// 💡 修正ポイント
// 現在のこの条件：

// if ($input_name && $input_pass)
// この式は
// → **「どちらも空でなければOK」**という意味にしかなりません。

// でも本来必要だったのは
// → 「中身が 'koh' と '1234' に一致しているか？」という比較です。


// 🎯 正しくはこうする必要があります：

// if ($input_name === "koh" && $input_pass === "1234") {
//     echo "ログイン成功<br>";
// } else {
//     echo "ログイン失敗<br>";
// }

// 🧠 補足解説（初心者向け）
// === は「値も型も完全に一致しているか」をチェックします。

// && は「両方が正しい場合だけ」処理を実行します。

// else は「それ以外全部」です。

// 💮 総評
// 構造は合っていて、あと比較の条件だけですね！
// if文の中で「具体的に何と何を比べるか」がとても大事なポイントです。

