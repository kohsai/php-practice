<?php
// 🎯 Day5 練習問題（strict_check.php）
//
// 📝 【問題】
// 入力された値が「特定の正解」と一致するかを、`===` を使って厳密に比較してください。
//
// 【条件】
// ✅ `$answer` に "coachtech" を代入
// ✅ `$input` に任意の値（例："coachtech" など）を代入
// ✅ 両者が完全に一致していれば → 「正解です」と表示
// ✅ それ以外は → 「間違いです」と表示
// ✅ 出力は `<br>` を使ってブラウザに表示すること
//
// 【ヒント】
// 💡 `===` を使うことで「値と型の両方」をチェックできます
//

// ✅ ここから下に『自分で考えた日本語の処理手順』をコメントで書いてください。
// （ここに自分で日本語の処理文を書いてください）
// `$answer` に "coachtech" を代入
// `$input` に任意の値（例："coachtech" など）を代入
// 両者が完全に一致していれば → 「正解です」と表示
// それ以外は → 「間違いです」と表示
// 出力は `<br>` を使ってブラウザに表示すること


// ✅ その下に『自分でPHPコード』を書いてください。
// （ここに自分でPHPコードを書いてください）

$answer = "coachtech";
$input = "coachtech";

if ($answer === "coachtech" && $input === "coachtech") {
    echo "正解です<br>";
} else {
    echo "間違いです<br>";
}



// 🔍 改善ポイント（コードの最適化）
// 現在のコードは次のように書かれています：


// if ($answer === "coachtech" && $input === "coachtech")
// この書き方は「どちらも 'coachtech' か？」という確認ですが、
// 実際にチェックしたいのは **「$answer と $input が一致するか？」**です。

// ✅ より適切な書き方はこちら：

// if ($answer === $input) {
//     echo "正解です<br>";
// } else {
//     echo "間違いです<br>";
// }
// 💡 なぜこの書き方がベターなのか？
// $answer に「正解値」をあらかじめ代入しているので、

// 比較対象の $input がそれと一致するかどうかだけを確認すれば良いからです。

// もし $answer の値が将来 "coachtech" 以外に変わっても、コードを修正しなくて済みます（＝メンテナンス性が高い）。

// 💮 総評
// 本質的な理解はしっかりできており、実行も成功しています！
// あとは「コードの簡潔さ・再利用性」に目を向けられると、より実務的な書き方ができますね。


