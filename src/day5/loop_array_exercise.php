<?php
// 🎯 Day5 練習問題（loop_array_exercise.php）
//
// 📝 【問題】
// 以下の配列から「偶数かつ10以上」の数字だけを抽出し、1行ずつ表示してください。
//
// 【条件】
// ✅ 配列：$numbers = [5, 12, 7, 20, 3, 8, 15, 30, 2];
// ✅ 偶数かつ10以上の数字のみ表示
// ✅ 表示形式：「対象の数字: ○○」
// ✅ 出力は <br> で改行すること
//
// 【ヒント】
// 💡 偶数判定：$num % 2 === 0
// 💡 10以上：$num >= 10
//
// 【日本語の処理文】
// ・配列から1つずつ数字を取り出す。
// ・その数字が偶数かどうか、その数字が10以上か確認する。
// ・条件を満たす数字だけ表示する。
// ・表示形式は「対象の数字: ○○」。出力に <br> をつける。
//
// 【コード記入枠】
//

$numbers = [5, 12, 7, 20, 3, 8, 15, 30, 2];

// 自分の回答：
// for ($numbers as $number) {
//     ($number % 2 === 0 && $number >=10) (
//         echo "対象の数字：{$number}<br>";
// }

// 採点と修正案：
// forではなく foreach を使うべき
// for構文はカウンタ付きループです

// 今回は配列を1つずつ処理するため、foreachが正解です

// 条件式の書き方が誤り
// 条件式のあとに () ではなく、if ( ... ) { ... } とする必要があります

// カッコの対応が取れていない
// { や } の開閉が正しくないため、構文エラーになります

foreach($numbers as $number) {
    if($number % 2 === 0 && $number >10) {
        echo "対象の数字:{$number}<br>";
    }
}

// 自分の反省：
// ・使うべき構文を理解していない。（forを使う? foreachを使う?そもそも使うべき構文を判断できていない）
// ・構文を理解しないので当然「条件式」も記述できない。（if...なども出てこない）
// ・カッコのミスに気づいていない。（{}と()の使い分けを分かっていないかも。）
